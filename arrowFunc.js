const sayHi = function (name, age) {
  console.log(`Hello ${name}, you are ${age} years old`);
};

sayHi("John", 24);

const hello = (name, age) => {
  console.log(`Hello ${name}, you are ${age} years old`);
};

hello("John", 25);
